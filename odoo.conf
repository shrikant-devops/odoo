[options]
; This is the main configuration file for Odoo

; Database connection settings
db_host = database-2.czc46mgoexep.ap-south-1.rds.amazonaws.com
db_port = 5432
db_user = shrikant
db_password = shrikant123
db_name = database-2

; Odoo server settings
addons_path = addons
admin_passwd = admin123
xmlrpc_port = 8069
longpolling_port = 8072

; Logging settings
logfile = /var/log/odoo/odoo.log
log_level = info

; Other settings
workers = 2
limit_memory_soft = 640000000
limit_memory_hard = 760000000
limit_time_cpu = 60
limit_time_real = 120

; Additional database settings
db_maxconn = 64
max_cron_threads = 2

; Security settings
list_db = True
db_template = template0

; Data directory
data_dir = /var/lib/odoo

; Proxy mode (if behind a reverse proxy)
proxy_mode = False

; CSV import/export settings
csv_internal_sep = ,

; Email settings (configure as needed)
; email_from = False
; smtp_server = localhost
; smtp_port = 25
; smtp_ssl = False
; smtp_user = False
; smtp_password = False

; Demo data (set to False for production)
without_demo = True

; Performance settings
unaccent = True
